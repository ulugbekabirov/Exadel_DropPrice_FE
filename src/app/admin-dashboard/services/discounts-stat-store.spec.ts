import { DiscountsStatStore } from './discounts-stat-store';

describe('DiscountsStatStore', () => {
  it('should create an instance', () => {
    expect(new DiscountsStatStore()).toBeTruthy();
  });
});
